> 生成工具：pg-ui-structure-html V1.6 | 生成时间：2026-08-28

# MCP 长期记忆系统原型说明

## 概述

- 项目/功能名称：MCP 长期记忆系统前端改版
- 原型生成日期：2026-08-28
- 输入来源：当前 Vue 管理页、用户提供的界面截图及已实现功能说明
- 输出目录：`docs/prototype/memory-console-redesign/`
- 原型性质：可点击的静态 HTML，不连接真实后端

## 页面清单

| 序号 | 页面名称 | 文件名 | 说明 |
| --- | --- | --- | --- |
| 1 | 记忆中心 | `index.html` | 概览、搜索、范围筛选、记忆列表与详情入口 |
| 2 | 写入记忆 | `write-memory.html` | 写入内容、选择全局/项目范围、填写会话与标签 |

## 导航关系

- 记忆中心 → 全部/全局/项目记忆：同页筛选状态。
- 记忆中心 → 写入记忆：通过顶部或侧边栏入口打开独立页面。
- 写入记忆 → 记忆中心：通过“取消”“返回记忆中心”或原型保存结果返回。
- 记忆列表 → 记忆详情：通过每行尾部的 `•••` 打开详情抽屉。

## 界面层级结构

- 顶层页面：记忆中心。
  - 同页状态：语义搜索、关键词搜索、混合搜索模式切换。
  - 同页状态：全部、全局、项目范围切换。
  - 子界面：记忆详情抽屉；从列表行尾部入口打开，通过关闭或取消返回。
    - 下一层：删除确认弹窗；从抽屉“删除”打开，通过取消或确认关闭。
- 顶层页面：写入记忆。
  - 同页状态：全局记忆与项目记忆选择；选择项目记忆时显示项目路径。

## 关键交互说明

- 搜索方式切换会更新当前选中状态并显示原型提示。
- 全局/项目范围切换会过滤示例列表。
- 详情抽屉保留列表页上下文，支持编辑、取消与删除入口演示。
- 删除操作展示二级确认弹窗。
- 写入页支持范围切换、字数统计和保存后的原型返回。
- 窄屏通过顶部菜单按钮展开侧边栏。

## 输入对照说明

| 原型内容 | 输入来源 | 处理方式 |
| --- | --- | --- |
| 记忆内容、用户 ID、会话 ID、标签 | 当前 Vue 页面 | 原字段实现 |
| 项目路径、全局/项目记忆 | 用户需求与当前 Vue 页面 | 重新组织信息层级 |
| 语义、关键词、混合搜索 | 当前 Vue 页面与 Elasticsearch/Milvus 功能 | 分段控件呈现 |
| 开始/结束日期、同时包含全局记忆 | 当前 Vue 页面 | 收纳至可折叠筛选区 |
| 编辑、删除、确认 | 当前 Vue 页面 | 详情抽屉及确认弹窗呈现 |
| 记忆列表与向量状态 | 当前 Vue 页面数据字段 | 卡片式列表呈现 |

## 保守处理与原型补充

- 为保证导航清楚，补充侧边栏、返回入口、搜索模式提示和移动端菜单；它们不承载新的业务规则。
- 概览数字与列表内容仅用于表现现有数据层级，不代表实时数据。
- 服务连接状态仅复用现有“后端在线”和技术组件信息，不实现诊断功能。
- 原型不发真实请求，保存、搜索、编辑和删除只显示演示反馈。

> 本文档由 pg-ui-structure-html V1.6 生成。
